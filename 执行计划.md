# Go 生产级 StableAgent 项目开发排期

## 一、排期假设

### 1. 项目目标

最终实现完整版生产级项目，包括：

* Go 后端服务；
* PostgreSQL 核心数据存储；
* Redis 缓存、锁、cancel flag、限流；
* Temporal 长任务工作流；
* Tool Gateway 工具调用网关；
* LLM Gateway 模型调用网关；
* StableAgent 可恢复执行；
* checkpoint；
* cancel / resume；
* 高风险工具审批；
* AgentEvent timeline；
* SSE / WebSocket 实时推送；
* OpenTelemetry trace；
* Prometheus metrics；
* Grafana dashboard；
* 多租户隔离；
* RBAC 权限；
* Outbox 可靠消息；
* Artifact 管理；
* Kubernetes 生产部署；
* CI/CD；
* 单元测试、集成测试、压测、故障恢复测试。

---

### 2. 时间周期

建议周期：**20 周**

适合以下情况：

* 1 名主力后端 + 1 名前端 + 1 名 DevOps，约 20 周；
* 如果只有 1 名全栈开发者，建议按 24 到 28 周执行；
* 如果是 4 到 6 人小团队，可以压缩到 12 到 16 周。

---

### 3. 每周交付节奏

每周固定产出：

```text
周一：设计 / 拆分 / 基础实现
周二：核心编码
周三：核心编码 + 联调
周四：测试 + 异常处理
周五：收尾 + 文档 + 演示 + 复盘
```

---

# 二、总体里程碑

| 阶段   |          周期 | 目标                                        |
| ---- | ----------: | ----------------------------------------- |
| 阶段 1 |   第 1 - 2 周 | 项目初始化、架构骨架、数据库基础                          |
| 阶段 2 |   第 3 - 5 周 | Task、Event、State、Timeline 基础闭环            |
| 阶段 3 |   第 6 - 8 周 | Temporal Runtime、Checkpoint、Cancel、Resume |
| 阶段 4 |  第 9 - 11 周 | LLM Gateway、Tool Gateway、审批机制             |
| 阶段 5 | 第 12 - 13 周 | Budget、LoopDetector、Artifact、Outbox       |
| 阶段 6 | 第 14 - 15 周 | 前端完整管理台                                   |
| 阶段 7 | 第 16 - 17 周 | 可观测性、安全、多租户、RBAC                          |
| 阶段 8 | 第 18 - 19 周 | 生产部署、压测、故障恢复测试                            |
| 阶段 9 |      第 20 周 | 总体验收、文档、演示、面试材料                           |

---

# 三、按周与每天任务划分

## 第 1 周：项目初始化与工程骨架

### 目标

完成 Go 项目基础结构、代码规范、基础服务启动、Docker 本地环境。

### 周一

* 创建 Git 仓库；
* 初始化 Go module；
* 设计 monorepo 目录结构；
* 创建 `cmd/api-service`；
* 创建 `cmd/runtime-worker`；
* 创建 `cmd/tool-gateway`；
* 创建 `cmd/llm-gateway`；
* 编写基础 README；
* 确定代码规范、分支规范、commit 规范。

### 周二

* 接入配置管理；
* 接入结构化日志；
* 接入统一错误码包；
* 实现 request_id middleware；
* 实现基础 health check；
* 实现 graceful shutdown。

### 周三

* 编写 Dockerfile；
* 编写 docker-compose；
* 启动 PostgreSQL；
* 启动 Redis；
* 启动 Temporal；
* 启动 MinIO；
* 启动 Jaeger / Tempo。

### 周四

* 搭建数据库 migration 工具；
* 创建基础 migration 目录；
* 创建本地开发脚本；
* 创建 Makefile；
* 添加 lint、fmt、test 命令；
* 接入 golangci-lint。

### 周五

* 整理项目启动文档；
* 验证本地一键启动；
* 验证四个服务可以正常启动；
* 输出第一周技术文档；
* 完成阶段演示：本地服务启动 + health check。

### 本周交付

* 项目骨架；
* Docker Compose 本地环境；
* 基础服务启动；
* 开发规范文档。

---

## 第 2 周：数据库核心模型与数据访问层

### 目标

完成核心表设计、migration、sqlc 查询、Repository 基础代码。

### 周一

* 创建 `agent_tasks` 表；
* 创建 `agent_states` 表；
* 创建 `agent_events` 表；
* 编写对应 migration；
* 设计状态枚举。

### 周二

* 创建 `tool_calls` 表；
* 创建 `checkpoints` 表；
* 创建 `approvals` 表；
* 创建 `artifacts` 表；
* 添加关键索引。

### 周三

* 创建 `task_outbox` 表；
* 创建 `tenant_tool_policies` 表；
* 创建 `users`、`tenants` 简化表；
* 设计 tenant 隔离字段；
* 编写数据约束。

### 周四

* 接入 pgx；
* 接入 sqlc；
* 编写基础 SQL query；
* 生成类型安全代码；
* 实现 TaskRepository；
* 实现 EventRepository。

### 周五

* 实现 StateRepository；
* 实现 ToolCallRepository；
* 实现 CheckpointRepository；
* 编写 repository 单元测试；
* 验证 migration up/down；
* 输出数据库设计文档。

### 本周交付

* 核心数据库表；
* 类型安全 SQL；
* Repository 层；
* 数据库设计文档。

---

## 第 3 周：Task API 与任务创建闭环

### 目标

实现任务创建、任务查询、任务列表、基础鉴权和多租户上下文。

### 周一

* 实现 JWT 解析 middleware；
* 实现 tenant context；
* 实现 user context；
* 设计统一 API response；
* 设计统一错误返回格式。

### 周二

* 实现 `POST /api/v1/tasks`；
* 校验 goal；
* 校验 budget；
* 生成 task_id；
* 创建 agent_task；
* 创建 agent_state；
* 写入 TASK_CREATED event。

### 周三

* 实现 `GET /api/v1/tasks/{task_id}`；
* 实现 task 权限校验；
* 实现 tenant 隔离查询；
* 实现 task status 转换展示；
* 编写 API 测试。

### 周四

* 实现 `GET /api/v1/tasks`；
* 支持 status 过滤；
* 支持 created_at 排序；
* 支持分页；
* 注意分页不是亮点，但必须生产可用。

### 周五

* 添加 OpenAPI 文档；
* 添加接口集成测试；
* 添加 Postman / Bruno 调试集合；
* 完成任务创建到查询的闭环演示；
* 输出 API 文档第一版。

### 本周交付

* 任务创建 API；
* 任务详情 API；
* 任务列表 API；
* 基础鉴权和 tenant 隔离。

---

## 第 4 周：AgentEvent 与 Timeline 查询

### 目标

实现事件记录、timeline 查询、SSE 实时推送基础能力。

### 周一

* 设计 AgentEvent 类型枚举；
* 实现 EventService；
* 实现 append-only event 写入；
* 统一 event metadata 结构；
* 关联 trace_id、span_id 字段。

### 周二

* 实现 `GET /api/v1/tasks/{task_id}/events`；
* 支持 after_event_id；
* 支持 limit；
* 支持 type 过滤；
* 保证 event_id 顺序。

### 周三

* 实现 SSE endpoint；
* 支持 `Last-Event-ID`；
* 实现客户端断线重连逻辑；
* 实现 event 推送 channel；
* 防止慢客户端阻塞服务。

### 周四

* 接入 Redis Pub/Sub 或 NATS；
* 实现多实例 event 推送；
* 解决 API 多副本场景下 SSE 收不到事件的问题；
* 编写 SSE 集成测试。

### 周五

* 实现 timeline 前端原型；
* 展示 TASK_CREATED、STEP_STARTED、STEP_COMPLETED；
* 完成任务创建后 timeline 可见；
* 输出 Event Timeline 设计文档。

### 本周交付

* AgentEvent 写入；
* Timeline 查询；
* SSE 实时推送；
* Timeline 前端原型。

---

## 第 5 周：AgentState 与基础 Runtime 状态机

### 目标

实现不依赖 Temporal 的最小 Runtime 状态机，为后续接入 Temporal 打基础。

### 周一

* 定义 AgentState 领域模型；
* 定义 current_plan；
* 定义 current_step；
* 定义 memory_summary；
* 定义 artifacts；
* 定义 version 乐观锁。

### 周二

* 实现 StateService；
* 实现 state load；
* 实现 state update；
* 实现 version conflict 处理；
* 编写状态更新测试。

### 周三

* 实现 Runtime 主接口；
* 实现 `RunTask(ctx, taskID)`；
* 加载 task；
* 加载 state；
* 写入 TASK_STARTED event；
* 写入 STEP_STARTED event。

### 周四

* 实现模拟 LLM 输出；
* 实现模拟 step 推进；
* 实现 STEP_COMPLETED event；
* 更新 current_step；
* 完成 task status 从 QUEUED 到 RUNNING 到 COMPLETED。

### 周五

* 实现基础 Worker；
* 手动触发执行任务；
* 完成最小 Agent 执行闭环；
* 编写 Runtime 单元测试；
* 输出 Runtime 状态机文档。

### 本周交付

* AgentState；
* 基础 Runtime；
* 模拟 Agent 执行；
* Task 从创建到完成的最小闭环。

---

## 第 6 周：Temporal 接入与长任务 Workflow

### 目标

用 Temporal 替代简单 Worker，建立生产级长任务执行框架。

### 周一

* 设计 `AgentTaskWorkflow`；
* 设计 Activity 划分；
* 接入 Temporal Go SDK；
* 创建 Temporal client；
* 创建 worker bootstrap。

### 周二

* 实现 `StartAgentTaskWorkflow`；
* 使用 task_id 作为 workflow_id；
* 创建 workflow task queue；
* API 创建任务后启动 workflow；
* 处理 workflow already started。

### 周三

* 实现 `InitTaskActivity`；
* 实现 `LoadStateActivity`；
* 实现 `PlanStepActivity`；
* 实现 `UpdateStateActivity`；
* 实现 Activity timeout 配置。

### 周四

* 实现 workflow 主循环；
* 支持 step-by-step 执行；
* 支持 max step 初步限制；
* 写入 workflow started / completed event；
* 处理 Activity retry policy。

### 周五

* 验证 Worker 重启后 Workflow 不丢失；
* 验证 workflow_id 幂等；
* 编写 Temporal 集成测试；
* 输出 Temporal 工作流设计文档。

### 本周交付

* Temporal Workflow；
* Temporal Worker；
* 长任务异步执行；
* Worker 重启恢复基础能力。

---

## 第 7 周：Checkpoint 机制

### 目标

实现 LLM/tool call 前后的 checkpoint 保存与恢复基础能力。

### 周一

* 定义 checkpoint reason；
* 实现 checkpoint snapshot schema；
* 添加 schema_version；
* 实现 CheckpointService。

### 周二

* 在 LLM 调用前写 BEFORE_LLM checkpoint；
* 在 LLM 调用后写 AFTER_LLM checkpoint；
* 记录 state_version；
* 记录 event_offset；
* 记录 trace_id。

### 周三

* 在 tool call 前写 BEFORE_TOOL checkpoint；
* 在 tool call 后写 AFTER_TOOL checkpoint；
* 处理 checkpoint 写入失败；
* 添加 CHECKPOINT_CREATED event。

### 周四

* 实现 `GetLatestCheckpoint`；
* 实现 checkpoint restore；
* 恢复 AgentState；
* 校验 state_version；
* 处理 checkpoint 不存在。

### 周五

* 模拟 Worker 在不同阶段崩溃；
* 验证最近 checkpoint 查询；
* 验证恢复后 state 正确；
* 输出 checkpoint 设计文档；
* 完成 checkpoint 演示。

### 本周交付

* Checkpoint 保存；
* Checkpoint 查询；
* 基础恢复能力；
* 崩溃恢复测试。

---

## 第 8 周：Cancel / Resume / Pause

### 目标

实现任务取消、恢复、暂停，并与 Temporal workflow 打通。

### 周一

* 设计 cancel 状态流转；
* 实现 `POST /api/v1/tasks/{task_id}/cancel`；
* 写 Redis cancel flag；
* 更新 task status；
* 写 TASK_CANCEL_REQUESTED event。

### 周二

* Workflow 内支持 cancel signal；
* Activity 前检查 cancel flag；
* 在安全边界停止；
* 写 TASK_CANCELLED event；
* task status 改为 CANCELLED。

### 周三

* 实现 `POST /api/v1/tasks/{task_id}/resume`；
* 校验 task status；
* 加载最近 checkpoint；
* 恢复 state；
* 重新 signal workflow 或重启 workflow。

### 周四

* 实现 pause 状态；
* 支持人工暂停；
* 支持从 PAUSED resume；
* 处理 resume 幂等；
* 处理重复 cancel。

### 周五

* 编写 cancel/resume 集成测试；
* 测试 Worker 重启；
* 测试审批等待中的 cancel；
* 测试 completed 任务不可 resume；
* 输出 cancel/resume 文档。

### 本周交付

* Cancel；
* Resume；
* Pause；
* Workflow 恢复能力；
* 状态机完整性测试。

---

## 第 9 周：LLM Gateway

### 目标

实现生产级 LLM Gateway，统一模型调用、token 统计、超时、重试、成本记录。

### 周一

* 设计 LLM Gateway 接口；
* 定义 ChatCompletionRequest；
* 定义 LLMResponse；
* 定义 TokenUsage；
* 定义 ModelConfig。

### 周二

* 实现 OpenAI-compatible client；
* 实现 timeout；
* 实现 retry；
* 实现 provider error 映射；
* 记录 LLM_CALL_STARTED event。

### 周三

* 实现 token usage 统计；
* 实现 cost 估算；
* 更新 budget_usage；
* 记录 LLM_CALL_COMPLETED event；
* 写 AFTER_LLM checkpoint。

### 周四

* 实现模型路由；
* 支持默认模型；
* 支持按任务类型选择模型；
* 支持 fallback 模型；
* 支持 provider 限流。

### 周五

* 编写 LLM Gateway 测试；
* 使用 mock provider 做集成测试；
* 接入 Runtime；
* 让 Agent 使用真实 LLM 生成 plan；
* 输出 LLM Gateway 文档。

### 本周交付

* LLM Gateway；
* Token 统计；
* 成本统计；
* 模型路由；
* Runtime 接入真实 LLM。

---

## 第 10 周：Tool Gateway 基础能力

### 目标

实现 Tool Gateway、Tool Registry、参数校验、权限校验、幂等控制。

### 周一

* 设计 ToolGateway 接口；
* 设计 Tool Registry；
* 定义 ToolSpec；
* 定义 ToolRiskLevel；
* 定义 ToolExecutor。

### 周二

* 实现工具注册机制；
* 注册 read_file 工具；
* 注册 web_search mock 工具；
* 注册 create_draft mock 工具；
* 注册 send_email mock 工具。

### 周三

* 实现参数 schema 校验；
* 实现 arguments normalize；
* 实现 arguments_hash；
* 实现 idempotency_key 生成；
* 写入 tool_calls 表。

### 周四

* 实现工具权限校验；
* 实现 tenant_tool_policies；
* 实现 TOOL_NOT_ALLOWED；
* 实现 ToolCall status 流转；
* 写 TOOL_CALL_STARTED event。

### 周五

* 接入 Runtime；
* Agent 可以选择工具；
* 低风险工具自动执行；
* 工具结果写回 Agent；
* 输出 Tool Gateway 基础文档。

### 本周交付

* Tool Gateway；
* Tool Registry；
* 参数校验；
* 权限校验；
* 工具幂等基础能力。

---

## 第 11 周：高风险工具审批

### 目标

实现高风险工具暂停、审批、拒绝、审批后继续执行。

### 周一

* 设计审批策略；
* 实现 risk engine；
* 根据 tool_name 计算静态风险；
* 根据 arguments 计算动态风险；
* 生成 approval_reason。

### 周二

* 高风险工具写入 approvals；
* ToolCall 进入 WAITING_APPROVAL；
* Task 进入 WAITING_APPROVAL；
* 写 TOOL_APPROVAL_REQUIRED event；
* Workflow 等待 signal。

### 周三

* 实现 approve API；
* 实现 reject API；
* 校验审批人权限；
* 防止重复审批；
* 写 TOOL_APPROVED / TOOL_REJECTED event。

### 周四

* Workflow 接收 approval signal；
* approve 后继续执行工具；
* reject 后把拒绝结果返回给 Agent；
* Agent 调整 plan 或结束任务。

### 周五

* 实现审批前端页面；
* 展示工具名、参数、风险原因；
* 支持 approve/reject；
* 完成高风险工具审批演示；
* 输出审批设计文档。

### 本周交付

* 高风险审批；
* Workflow 等待和恢复；
* 审批 UI；
* 工具审批完整闭环。

---

## 第 12 周：Budget 与 LoopDetector

### 目标

实现 step、token、tool call、cost 限制，以及重复循环检测。

### 周一

* 实现 BudgetService；
* 检查 max_steps；
* 检查 max_tokens；
* 检查 max_tool_calls；
* 检查 max_cost_usd。

### 周二

* 每次 LLM 后更新 token；
* 每次 tool 后更新 tool call usage；
* 每个 step 后更新 used_steps；
* budget_usage 持久化。

### 周三

* 设计 step fingerprint；
* 实现 plan normalize；
* 实现 tool arguments normalize；
* 生成 fingerprint hash；
* 保存最近 N 个 fingerprint。

### 周四

* 实现重复循环检测；
* 实现连续失败检测；
* 实现相同错误检测；
* 触发 LOOP_DETECTED event；
* task status 改为 STOPPED_BY_LIMIT。

### 周五

* 前端展示预算面板；
* 展示已用 step/token/tool call；
* 展示停止原因；
* 编写 loop detector 测试；
* 输出 Budget 与 LoopDetector 文档。

### 本周交付

* Budget 控制；
* LoopDetector；
* STOPPED_BY_LIMIT；
* 预算面板。

---

## 第 13 周：Artifact 与 Outbox

### 目标

实现任务产物管理和可靠异步消息投递。

### 周一

* 设计 Artifact 模型；
* 实现 artifacts 表；
* 接入 S3 / MinIO；
* 支持保存文本产物；
* 支持保存 JSON 产物。

### 周二

* 支持大 output 自动转 artifact；
* Event 中只保存 artifact_id；
* 实现 artifact 下载接口；
* 实现 artifact 权限校验。

### 周三

* 实现 Outbox 写入；
* 创建任务时写 START_WORKFLOW outbox；
* event 推送时写 EVENT_PUBLISHED outbox；
* 避免 DB 成功但消息丢失。

### 周四

* 实现 outbox worker；
* 支持 retry_count；
* 支持 next_retry_at；
* 支持 dead letter；
* 支持幂等发送。

### 周五

* 集成测试：DB 写成功但消息失败；
* 验证 outbox 自动补偿；
* 前端展示 artifacts；
* 输出 Artifact 与 Outbox 文档。

### 本周交付

* Artifact 管理；
* S3 / MinIO 接入；
* Outbox；
* 可靠消息补偿。

---

## 第 14 周：前端任务管理台

### 目标

实现完整任务管理 UI。

### 周一

* 搭建 React + TypeScript 项目；
* 配置路由；
* 配置 API client；
* 配置登录态 mock；
* 配置 UI 组件库。

### 周二

* 实现任务创建页；
* 支持 goal 输入；
* 支持 budget 输入；
* 支持 constraints 输入；
* 创建成功跳转详情页。

### 周三

* 实现任务列表页；
* 支持状态过滤；
* 支持分页；
* 支持刷新；
* 展示任务状态、创建时间、当前 step。

### 周四

* 实现任务详情页；
* 展示基本信息；
* 展示 budget usage；
* 展示 current state；
* 展示操作按钮 cancel/resume。

### 周五

* 实现基础错误提示；
* 实现 loading 状态；
* 实现空状态；
* 前后端联调；
* 输出前端页面说明。

### 本周交付

* 任务创建页；
* 任务列表页；
* 任务详情页；
* cancel/resume 前端操作。

---

## 第 15 周：前端 Timeline、审批、Trace 页面

### 目标

完成生产级 UI 的核心交互。

### 周一

* 实现 Timeline 组件；
* 支持 event 分组；
* 支持不同 event 类型样式；
* 支持 event input/output 展开；
* 支持自动滚动。

### 周二

* 接入 SSE；
* 支持断线重连；
* 支持 Last-Event-ID；
* 支持实时追加 event；
* 处理重复 event 去重。

### 周三

* 实现审批页面；
* 展示待审批列表；
* 展示工具参数；
* 展示风险说明；
* 实现 approve/reject 操作。

### 周四

* 实现 Artifact 展示；
* 支持下载；
* 支持 JSON preview；
* 支持文本 preview；
* 支持大结果跳转 artifact。

### 周五

* 实现 Trace 跳转；
* 在 event 上展示 trace_id；
* 支持复制 task_id、trace_id；
* 完成端到端 UI 演示；
* 输出前端使用文档。

### 本周交付

* 实时 timeline；
* 审批 UI；
* Artifact UI；
* Trace 辅助入口。

---

## 第 16 周：认证、授权、多租户

### 目标

实现生产级身份认证、RBAC、多租户隔离。

### 周一

* 设计用户模型；
* 设计租户模型；
* 设计角色模型；
* 实现 JWT 认证；
* 实现 auth middleware。

### 周二

* 实现 RBAC；
* 定义 USER；
* 定义 TENANT_ADMIN；
* 定义 AUDITOR；
* 定义 SYSTEM_WORKER；
* 给 API 添加权限控制。

### 周三

* 所有 Repository 查询加 tenant_id；
* 所有 API 校验 task ownership；
* Worker 执行前校验 tenant；
* Tool Gateway 校验 tenant policy。

### 周四

* 实现 OIDC 接入预留；
* 实现 API Key 预留；
* 实现 service account 预留；
* 实现密钥不落日志；
* 敏感参数脱敏。

### 周五

* 编写权限测试；
* 测试跨租户访问；
* 测试无权限审批；
* 测试 auditor 只读；
* 输出安全设计文档。

### 本周交付

* JWT；
* RBAC；
* 多租户隔离；
* 权限测试；
* 安全文档。

---

## 第 17 周：OpenTelemetry、Metrics、日志、审计

### 目标

实现完整可观测能力。

### 周一

* 接入 OpenTelemetry SDK；
* API service 添加 HTTP tracing；
* Runtime Worker 添加 tracing；
* Tool Gateway 添加 tracing；
* LLM Gateway 添加 tracing。

### 周二

* 定义 span 命名规范；
* task.create；
* agent.step；
* llm.call；
* tool.call；
* checkpoint.create；
* approval.wait；
* artifact.save。

### 周三

* 接入 Prometheus metrics；
* 统计 task_total；
* 统计 task_success_total；
* 统计 task_failed_total；
* 统计 llm_tokens_total；
* 统计 tool_call_duration；
* 统计 workflow_duration。

### 周四

* 统一结构化日志字段；
* 添加 task_id；
* 添加 tenant_id；
* 添加 user_id；
* 添加 trace_id；
* 添加 span_id；
* 添加 event_type。

### 周五

* 创建 Grafana Dashboard；
* 创建关键告警；
* 任务失败率告警；
* 工具失败率告警；
* LLM 延迟告警；
* 输出可观测性文档。

### 本周交付

* OpenTelemetry trace；
* Prometheus metrics；
* 结构化日志；
* Grafana dashboard；
* 告警规则。

---

## 第 18 周：Kubernetes 生产部署

### 目标

完成完整生产部署环境。

### 周一

* 编写各服务 Dockerfile；
* 优化镜像体积；
* 使用非 root 用户；
* 添加 health check；
* 添加 readiness probe。

### 周二

* 编写 Kubernetes Deployment；
* 编写 Service；
* 编写 ConfigMap；
* 编写 Secret；
* 编写 Ingress。

### 周三

* 编写 Helm Chart；
* 支持不同环境 values；
* 支持 dev/staging/prod；
* 支持服务副本数配置；
* 支持资源限制配置。

### 周四

* 部署 PostgreSQL / 云数据库连接；
* 部署 Redis；
* 部署 Temporal；
* 部署 MinIO / S3 配置；
* 部署 OTel Collector。

### 周五

* 完成 staging 环境部署；
* 验证服务健康；
* 验证任务创建和执行；
* 验证 timeline；
* 输出部署文档。

### 本周交付

* Docker 镜像；
* Kubernetes 配置；
* Helm Chart；
* Staging 环境。

---

## 第 19 周：测试、压测、故障恢复、安全加固

### 目标

达到生产上线标准。

### 周一

* 补充单元测试；
* Repository 测试；
* Service 测试；
* Runtime 测试；
* Tool Gateway 测试。

### 周二

* 编写集成测试；
* task 创建到完成；
* 高风险审批；
* cancel；
* resume；
* checkpoint 恢复。

### 周三

* 故障恢复测试；
* Worker 崩溃；
* LLM 调用超时；
* Tool Gateway 超时；
* DB 短暂不可用；
* Redis 短暂不可用；
* Temporal Worker 重启。

### 周四

* 压测；
* 并发创建任务；
* 多 Worker 执行；
* Timeline SSE 长连接；
* Tool Gateway QPS；
* LLM Gateway 限流。

### 周五

* 安全加固；
* 检查敏感日志；
* 检查跨租户访问；
* 检查审批绕过；
* 检查幂等重复调用；
* 输出测试报告。

### 本周交付

* 单元测试；
* 集成测试；
* 压测报告；
* 故障恢复测试报告；
* 安全检查报告。

---

## 第 20 周：最终验收、文档、演示、面试材料

### 目标

完成生产级项目交付。

### 周一

* 整理 README；
* 整理架构文档；
* 整理数据库文档；
* 整理 API 文档；
* 整理部署文档。

### 周二

* 整理核心流程图；
* 绘制任务执行流程；
* 绘制 Tool Gateway 流程；
* 绘制 checkpoint/resume 流程；
* 绘制审批流程；
* 绘制 trace 关联流程。

### 周三

* 准备演示数据；
* 准备普通任务演示；
* 准备高风险审批演示；
* 准备 Worker 崩溃恢复演示；
* 准备 loop 超限演示。

### 周四

* 整理面试讲解稿；
* 整理技术难点；
* 整理项目亮点；
* 整理可追问问题；
* 整理优化前后指标。

### 周五

* 最终验收；
* 修复遗留问题；
* 打版本 tag；
* 输出 release note；
* 完成最终项目演示。

### 本周交付

* 完整项目源码；
* 完整部署环境；
* 完整文档；
* 演示脚本；
* 面试材料；
* 最终版本 release。

---

# 四、最终验收标准

## 1. 功能验收

必须满足：

```text
用户可以创建长任务
系统返回 task_id
Agent 可以分 step 执行
每一步都有 AgentEvent
LLM 调用前后有 checkpoint
Tool 调用前后有 checkpoint
工具调用必须经过 Tool Gateway
高风险工具必须等待审批
审批通过后任务继续执行
审批拒绝后 Agent 可以调整计划
任务可以 cancel
任务可以 resume
失败后可以从最近 checkpoint 恢复
UI 可以展示完整 timeline
UI 可以展示工具审批
UI 可以展示 artifacts
系统可以检测 step 超限
系统可以检测 token 超限
系统可以检测 tool call 超限
系统可以检测重复循环
```

---

## 2. 生产级验收

必须满足：

```text
所有服务支持 graceful shutdown
所有服务支持 health check
所有服务接入 OpenTelemetry
所有关键操作有结构化日志
所有关键指标进入 Prometheus
Grafana 有 dashboard
支持 Kubernetes 部署
支持 Helm 配置
支持多租户隔离
支持 RBAC
支持敏感字段脱敏
支持 outbox 消息补偿
支持工具调用幂等
支持 Worker 横向扩容
支持 Temporal Workflow 恢复
支持故障恢复测试
支持压测报告
```

---

## 3. 关键测试场景

| 场景                   | 预期                  |
| -------------------- | ------------------- |
| 创建任务后 API 服务重启       | 任务不丢失               |
| Runtime Worker 执行中崩溃 | Temporal 恢复执行       |
| LLM 调用后 Worker 崩溃    | 恢复后不重复消耗已保存结果       |
| Tool 调用后 Worker 崩溃   | 不重复执行副作用工具          |
| 高风险工具调用              | 必须进入审批              |
| 审批通过                 | Workflow 继续执行       |
| 审批拒绝                 | Agent 收到拒绝结果        |
| 用户 cancel            | 任务进入 CANCELLED      |
| 用户 resume            | 从 checkpoint 恢复     |
| Agent 无限循环           | 触发 LOOP_DETECTED    |
| token 超限             | 触发 STOPPED_BY_LIMIT |
| 跨租户访问                | 返回 403              |
| 重复审批                 | 只生效一次               |
| 重复工具调用               | idempotency_key 拦截  |

---

# 五、建议实现顺序总结

最推荐的实现顺序是：

```text
1. 工程骨架
2. 数据库模型
3. Task API
4. Event Timeline
5. AgentState
6. Temporal Workflow
7. Checkpoint
8. Cancel / Resume
9. LLM Gateway
10. Tool Gateway
11. 高风险审批
12. Budget / LoopDetector
13. Artifact / Outbox
14. 前端管理台
15. 多租户 / RBAC
16. OpenTelemetry / Metrics / Logs
17. Kubernetes 部署
18. 测试 / 压测 / 故障恢复
19. 文档 / 演示 / 面试材料
```