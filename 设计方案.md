# StableAgent：生产级通用 Agent 执行平台设计方案

## 一、项目定位

### 1. 一句话介绍

StableAgent 是一个以稳定性为核心的生产级通用 Agent 平台，支持长任务可恢复执行、工具调用安全治理、人工审批、执行 timeline、checkpoint、cancel/resume 和完整可观测 trace。

---

## 二、生产级技术选型

### 1. 总体技术栈

| 模块       | 技术                     |    是否必须 | 选择理由                                              |
| -------- | ---------------------- | ------: | ------------------------------------------------- |
| 后端语言     | Go 1.26.x              |      必须 | 并发模型简单，性能稳定，适合 Worker、Gateway、Runtime             |
| HTTP 框架  | chi / Gin / Fiber      |      可选 | Go 标准库可用；生产项目建议 chi，轻量、兼容 net/http                |
| RPC      | gRPC                   |      推荐 | 内部服务间调用更规范，适合 Runtime、Tool Gateway、Worker         |
| 工作流引擎    | Temporal               | 生产级推荐必选 | 长任务、重试、超时、恢复、幂等、异步执行能力成熟                          |
| 数据库      | PostgreSQL             |      必须 | 事务可靠，JSONB 适合存储 AgentEvent、AgentState、ToolCall 参数 |
| 数据访问     | pgx + sqlc             |      必须 | 类型安全 SQL，性能好，可控性强                                 |
| 数据迁移     | goose / golang-migrate |      必须 | 版本化管理数据库结构                                        |
| 缓存/锁     | Redis                  |      必须 | cancel flag、分布式锁、限流、短期状态缓存                        |
| 消息队列     | NATS JetStream / Kafka |      必须 | 事件投递、timeline 推送、异步解耦                             |
| 任务编排     | Temporal Workflow      |   生产级推荐 | 用于长任务生命周期、Activity 重试、超时和恢复                       |
| 可观测      | OpenTelemetry          |      必须 | trace、metrics、logs 串联 task、step、tool call         |
| 日志       | zap / slog             |      必须 | 结构化日志，便于 ELK / Loki 查询                            |
| 指标       | Prometheus + Grafana   |      必须 | 监控任务成功率、失败率、token、工具耗时                            |
| Trace 后端 | Tempo / Jaeger         |      推荐 | 查看完整链路                                            |
| 对象存储     | S3 / MinIO             |      必须 | 保存 artifacts、大输出、报告、工具结果文件                        |
| API 网关   | Envoy / Nginx / Kong   |      可选 | 统一入口、限流、TLS、鉴权                                    |
| 部署       | Docker + Kubernetes    |    生产必选 | Worker 横向扩展、滚动发布、资源隔离                             |
| 配置       | Viper / envconfig      |      可选 | 统一管理环境变量和配置                                       |
| 密钥管理     | Vault / KMS            |    生产必选 | 保存 LLM key、工具 token、第三方凭证                         |

---

## 三、生产级总体架构

### 1. 服务拆分

生产级不建议把所有逻辑塞进一个单体服务。推荐拆成以下服务：

```text
stableagent/
├── api-service              # 用户 API，任务创建、查询、审批、取消、恢复
├── runtime-service          # StableAgent 核心状态机
├── worker-service           # 执行 Temporal Activity / 后台任务
├── tool-gateway-service     # 工具调用网关，风险判断、权限、审批、幂等
├── llm-gateway-service      # LLM 统一入口，模型路由、token 统计、重试
├── event-service            # AgentEvent 写入、查询、timeline 推送
├── approval-service         # 高风险工具审批
├── artifact-service         # 任务产物管理
├── observability-service    # trace、metrics、audit 查询，可与其他服务合并
└── web-ui                   # 前端管理台
```

MVP 可以合并为：

```text
api-service + worker-service + tool-gateway-service
```

生产级建议至少拆成：

```text
api-service
runtime-worker
tool-gateway
llm-gateway
```

---

### 2. 架构图

```text
User Browser
   |
   | HTTP / SSE / WebSocket
   v
API Service
   |
   | Create Task / Cancel / Resume / Approve
   v
PostgreSQL + Redis
   |
   v
Temporal Server
   |
   v
Runtime Worker
   |
   | LLM Request
   v
LLM Gateway
   |
   | Tool Request
   v
Tool Gateway
   |
   | Risk Check / Permission / Idempotency / Approval
   v
External Tools

Event Stream:
Runtime Worker -> Event Service -> PostgreSQL -> NATS / Redis PubSub -> UI Timeline

Observability:
All Services -> OpenTelemetry Collector -> Tempo / Jaeger / Prometheus / Grafana
```

---

## 四、核心模块设计

## 1. API Service

### 职责

* 用户登录后的任务 API；
* 创建任务；
* 查询任务；
* cancel / resume；
* 工具审批；
* 查询 timeline；
* 查询 artifacts；
* 多租户鉴权。

### Go 技术实现

推荐：

```text
net/http + chi
JWT middleware
RBAC middleware
Request ID middleware
OpenTelemetry middleware
zap / slog structured logging
```

### 生产级要求

* 所有请求必须带 `request_id`；
* 所有查询必须校验 `tenant_id`；
* 所有写操作必须做权限校验；
* 所有接口必须有超时控制；
* 所有接口返回统一错误码；
* 审批、cancel、resume 必须幂等。

---

## 2. Runtime Worker

### 职责

* 执行 Agent 主循环；
* 加载 AgentState；
* 生成 plan；
* 执行 step；
* 调用 LLM Gateway；
* 调用 Tool Gateway；
* 写入 checkpoint；
* 检测 step/token/loop 超限；
* 处理 cancel/resume；
* 生成 artifacts。

### Go 生产级设计

Worker 不应该直接使用无限 goroutine，而是使用受控并发：

```go
type WorkerConfig struct {
    MaxConcurrentTasks int
    StepTimeout        time.Duration
    LLMTimeout         time.Duration
    ToolTimeout        time.Duration
    MaxRetries         int
}
```

每个任务执行时必须传递 `context.Context`：

```go
func (r *Runtime) RunTask(ctx context.Context, taskID string) error
```

关键点：

* `ctx` 用于 cancel；
* 每个 step 有超时；
* LLM call 有单独超时；
* tool call 有单独超时；
* DB 事务不能跨外部网络调用；
* checkpoint 必须在外部调用前后落库；
* Worker 重启后任务不能丢。

生产级推荐用 Temporal 管理任务生命周期：

```text
AgentTaskWorkflow
  -> InitStateActivity
  -> PlanStepActivity
  -> LLMCallActivity
  -> ToolCallActivity
  -> CheckpointActivity
  -> BudgetCheckActivity
  -> LoopDetectActivity
```

---

## 3. LLM Gateway

### 职责

* 统一封装所有模型调用；
* 模型路由；
* prompt 版本管理；
* token 统计；
* 超时控制；
* 重试；
* 限流；
* 记录 LLM call event；
* 屏蔽不同模型 provider 差异。

### 是否必须独立成服务

MVP 可以放在 worker 内。

生产级建议独立：

```text
llm-gateway-service
```

原因：

* 多模型接入更方便；
* 成本统计集中；
* token 限流集中；
* prompt 审计集中；
* 模型降级更容易。

---

## 4. Tool Gateway

### 职责

所有工具调用必须经过 Tool Gateway。

它负责：

* 工具注册；
* 参数 schema 校验；
* 权限校验；
* 风险等级识别；
* 审批判断；
* 幂等控制；
* 工具执行；
* 工具结果记录；
* 工具调用审计。

### 工具风险等级

```text
LOW       只读工具，例如搜索、读取文档
MEDIUM    可回滚写操作，例如创建草稿、创建临时文件
HIGH      外部可见或不可逆操作，例如发送邮件、删除文件、修改数据库
CRITICAL  涉及资金、生产配置、批量删除、权限变更
```

### 生产级规则

| 风险等级     | 处理方式           |
| -------- | -------------- |
| LOW      | 自动执行           |
| MEDIUM   | 根据租户策略决定是否审批   |
| HIGH     | 必须人工审批         |
| CRITICAL | 默认禁止，企业版支持多级审批 |

### Go 结构示例

```go
type ToolCallRequest struct {
    TaskID         string                 `json:"task_id"`
    ToolName       string                 `json:"tool_name"`
    Arguments      map[string]any         `json:"arguments"`
    IdempotencyKey string                 `json:"idempotency_key"`
    UserID         string                 `json:"user_id"`
    TenantID       string                 `json:"tenant_id"`
}

type ToolCallResult struct {
    CallID         string         `json:"call_id"`
    Status         ToolCallStatus `json:"status"`
    RiskLevel      RiskLevel      `json:"risk_level"`
    ApprovalStatus ApprovalStatus `json:"approval_status"`
    Result         map[string]any `json:"result,omitempty"`
}
```

---

## 五、核心调用流程

## 1. 创建任务流程

```text
POST /api/v1/tasks
  -> AuthMiddleware 解析 user_id、tenant_id
  -> TaskService 校验预算
  -> DB transaction:
       insert agent_task
       insert agent_state
       insert agent_event: TASK_CREATED
  -> Start Temporal Workflow / 投递队列
  -> 返回 task_id
```

### 关键要求

* 创建任务必须事务化；
* task_id 必须全局唯一；
* 如果启动 workflow 失败，需要使用 outbox 补偿；
* 不允许 API 请求阻塞等待任务完成。

---

## 2. Agent 执行流程

```text
Runtime Worker 启动 task
  -> 加载 AgentTask
  -> 加载 AgentState
  -> 检查 status
  -> 检查 cancel flag
  -> 检查 budget
  -> 写 STEP_STARTED event
  -> 写 BEFORE_LLM checkpoint
  -> 调用 LLM Gateway
  -> 写 AFTER_LLM checkpoint
  -> 解析 LLM 输出
  -> 如果需要工具:
       -> 写 BEFORE_TOOL checkpoint
       -> 调用 Tool Gateway
       -> 如果需要审批:
            task.status = WAITING_APPROVAL
            workflow wait signal
       -> 如果工具完成:
            写 AFTER_TOOL checkpoint
  -> 更新 AgentState version
  -> 写 STEP_COMPLETED event
  -> 检查是否完成
```

---

## 3. 高风险工具审批流程

```text
Agent 请求 tool call
  -> Tool Gateway 校验参数
  -> 计算 risk_level = HIGH
  -> insert tool_call status=WAITING_APPROVAL
  -> insert approval status=PENDING
  -> insert event TOOL_APPROVAL_REQUIRED
  -> task.status = WAITING_APPROVAL
  -> UI 展示审批卡片
  -> 用户 approve / reject
  -> approve:
       update approval APPROVED
       update tool_call APPROVED
       Temporal signal workflow
  -> reject:
       update approval REJECTED
       update tool_call REJECTED
       Temporal signal workflow
```

---

## 4. Resume 流程

```text
POST /api/v1/tasks/{task_id}/resume
  -> 校验权限
  -> 校验 task.status in FAILED / PAUSED / STOPPED_BY_LIMIT
  -> 查询最近 checkpoint
  -> 恢复 AgentState
  -> 标记 task.status = QUEUED
  -> 发送 Temporal signal 或重新启动 workflow
  -> 写 TASK_RESUMED event
```

### 关键要求

resume 时必须检查：

* 最近 checkpoint 是否存在；
* checkpoint 对应的 state_version 是否有效；
* checkpoint 之后是否有未完成 ToolCall；
* ToolCall 是否已经执行过；
* 有副作用的工具是否有幂等记录。

---

## 5. Cancel 流程

```text
POST /api/v1/tasks/{task_id}/cancel
  -> 校验权限
  -> task.status = CANCEL_REQUESTED
  -> Redis 写 cancel flag
  -> Temporal cancel workflow 或发送 cancel signal
  -> Worker 在 step 边界停止
  -> task.status = CANCELLED
  -> 写 TASK_CANCELLED event
```

### 生产级注意点

不能在任意位置强杀 goroutine。

应该在安全边界停止：

* step 开始前；
* LLM call 前；
* tool call 前；
* checkpoint 后；
* Activity 重试前。

---

# 六、数据库设计

## 1. 核心表

生产级建议表名使用复数或统一前缀。这里使用统一前缀：

```text
agent_tasks
agent_events
agent_states
tool_calls
checkpoints
approvals
artifacts
tenant_tool_policies
task_outbox
```

---

## 2. agent_tasks

```sql
create table agent_tasks (
    task_id varchar(64) primary key,
    user_id varchar(64) not null,
    tenant_id varchar(64) not null,
    goal text not null,
    status varchar(32) not null,
    budget jsonb not null default '{}',
    budget_usage jsonb not null default '{}',
    workflow_id varchar(128),
    trace_id varchar(128),
    last_error_code varchar(64),
    last_error_message text,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now()
);

create index idx_agent_tasks_tenant_user_status
on agent_tasks (tenant_id, user_id, status, created_at desc);

create index idx_agent_tasks_status_updated
on agent_tasks (status, updated_at desc);
```

---

## 3. agent_events

```sql
create table agent_events (
    event_id bigserial primary key,
    task_id varchar(64) not null references agent_tasks(task_id),
    tenant_id varchar(64) not null,
    type varchar(64) not null,
    input jsonb,
    output jsonb,
    metadata jsonb,
    trace_id varchar(128),
    span_id varchar(128),
    created_at timestamptz not null default now()
);

create index idx_agent_events_task_event
on agent_events (task_id, event_id);

create index idx_agent_events_tenant_type_time
on agent_events (tenant_id, type, created_at desc);
```

生产级要求：

* event 只追加，不修改；
* event output 大于阈值时写入对象存储；
* event 表需要按时间或 tenant 分区；
* timeline 查询走 `(task_id, event_id)` 索引。

---

## 4. agent_states

```sql
create table agent_states (
    task_id varchar(64) primary key references agent_tasks(task_id),
    tenant_id varchar(64) not null,
    current_plan jsonb not null default '{}',
    current_step int not null default 0,
    memory_summary text,
    constraints jsonb not null default '{}',
    artifacts jsonb not null default '[]',
    loop_fingerprints jsonb not null default '[]',
    version int not null default 1,
    updated_at timestamptz not null default now()
);

create index idx_agent_states_tenant
on agent_states (tenant_id);
```

更新 state 时必须带 version：

```sql
update agent_states
set current_step = $1,
    current_plan = $2,
    version = version + 1,
    updated_at = now()
where task_id = $3
  and version = $4;
```

如果 affected rows = 0，说明存在并发冲突。

---

## 5. tool_calls

```sql
create table tool_calls (
    call_id varchar(64) primary key,
    task_id varchar(64) not null references agent_tasks(task_id),
    tenant_id varchar(64) not null,
    user_id varchar(64) not null,
    tool_name varchar(128) not null,
    arguments jsonb not null,
    arguments_hash varchar(128) not null,
    idempotency_key varchar(160) not null,
    status varchar(32) not null,
    result jsonb,
    result_artifact_id varchar(64),
    risk_level varchar(16) not null,
    approval_status varchar(32) not null,
    error_code varchar(64),
    error_message text,
    started_at timestamptz,
    finished_at timestamptz,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now(),
    unique (tenant_id, idempotency_key)
);

create index idx_tool_calls_task_status
on tool_calls (task_id, status);

create index idx_tool_calls_tenant_tool_time
on tool_calls (tenant_id, tool_name, created_at desc);
```

---

## 6. checkpoints

```sql
create table checkpoints (
    checkpoint_id varchar(64) primary key,
    task_id varchar(64) not null references agent_tasks(task_id),
    tenant_id varchar(64) not null,
    state_version int not null,
    event_offset bigint not null,
    state_snapshot jsonb not null,
    reason varchar(64) not null,
    trace_id varchar(128),
    created_at timestamptz not null default now()
);

create index idx_checkpoints_task_created
on checkpoints (task_id, created_at desc);
```

生产级策略：

* 最近 20 个 checkpoint 保留热数据；
* 更早 checkpoint 可以归档到对象存储；
* checkpoint snapshot 要记录 schema_version；
* 恢复时要做 schema 兼容处理。

---

## 7. approvals

```sql
create table approvals (
    approval_id varchar(64) primary key,
    task_id varchar(64) not null references agent_tasks(task_id),
    tenant_id varchar(64) not null,
    call_id varchar(64) not null references tool_calls(call_id),
    approver_id varchar(64),
    status varchar(32) not null,
    risk_level varchar(16) not null,
    approval_reason text,
    comment text,
    expires_at timestamptz,
    decided_at timestamptz,
    created_at timestamptz not null default now(),
    unique (call_id)
);

create index idx_approvals_tenant_status
on approvals (tenant_id, status, created_at desc);

create index idx_approvals_approver_status
on approvals (approver_id, status, created_at desc);
```

---

## 8. task_outbox

生产级必须考虑：DB 写成功，但消息/Workflow 启动失败。

用 outbox 解决。

```sql
create table task_outbox (
    outbox_id bigserial primary key,
    tenant_id varchar(64) not null,
    aggregate_type varchar(64) not null,
    aggregate_id varchar(64) not null,
    event_type varchar(64) not null,
    payload jsonb not null,
    status varchar(32) not null default 'PENDING',
    retry_count int not null default 0,
    next_retry_at timestamptz,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now()
);

create index idx_task_outbox_status_retry
on task_outbox (status, next_retry_at);
```

---

# 七、API 设计

## 1. 创建任务

```http
POST /api/v1/tasks
Authorization: Bearer <jwt>
Content-Type: application/json
```

请求：

```json
{
  "goal": "分析需求文档并生成生产级 Go 技术方案",
  "budget": {
    "max_steps": 50,
    "max_tokens": 200000,
    "max_tool_calls": 40,
    "max_cost_usd": 10
  },
  "constraints": {
    "language": "zh-CN",
    "require_approval_for_high_risk_tools": true
  }
}
```

响应：

```json
{
  "task_id": "task_01JZK9X2R6Z8M9",
  "status": "QUEUED",
  "created_at": "2026-07-07T10:00:00+09:00"
}
```

---

## 2. 查询任务详情

```http
GET /api/v1/tasks/{task_id}
Authorization: Bearer <jwt>
```

响应：

```json
{
  "task_id": "task_01JZK9X2R6Z8M9",
  "goal": "分析需求文档并生成生产级 Go 技术方案",
  "status": "RUNNING",
  "current_step": 8,
  "budget": {
    "max_steps": 50,
    "max_tokens": 200000
  },
  "budget_usage": {
    "used_steps": 8,
    "used_tokens": 32800,
    "used_tool_calls": 4
  },
  "trace_id": "trace_abc",
  "created_at": "2026-07-07T10:00:00+09:00",
  "updated_at": "2026-07-07T10:04:00+09:00"
}
```

---

## 3. 查询 timeline

```http
GET /api/v1/tasks/{task_id}/events?after_event_id=0&limit=100
Authorization: Bearer <jwt>
```

响应：

```json
{
  "items": [
    {
      "event_id": 1,
      "type": "TASK_CREATED",
      "input": {
        "goal": "分析需求文档并生成生产级 Go 技术方案"
      },
      "output": null,
      "metadata": {
        "user_id": "user_001"
      },
      "trace_id": "trace_abc",
      "span_id": "span_001",
      "created_at": "2026-07-07T10:00:00+09:00"
    }
  ],
  "next_after_event_id": 1
}
```

---

## 4. SSE 实时事件

```http
GET /api/v1/tasks/{task_id}/events/stream
Authorization: Bearer <jwt>
```

SSE 示例：

```text
event: agent_event
data: {"event_id":12,"type":"STEP_COMPLETED","task_id":"task_01JZK9X2R6Z8M9"}
```

生产级要求：

* 支持断线重连；
* 支持 `Last-Event-ID`；
* 服务端不能无限缓存；
* event 写 DB 后再推送；
* 推送失败不影响任务执行。

---

## 5. 审批工具调用

```http
POST /api/v1/tool-calls/{call_id}/approve
Authorization: Bearer <jwt>
Content-Type: application/json
```

请求：

```json
{
  "comment": "允许发送该邮件"
}
```

响应：

```json
{
  "call_id": "call_01JZKA",
  "approval_status": "APPROVED",
  "task_status": "QUEUED"
}
```

---

## 6. 取消任务

```http
POST /api/v1/tasks/{task_id}/cancel
Authorization: Bearer <jwt>
```

响应：

```json
{
  "task_id": "task_01JZK9X2R6Z8M9",
  "status": "CANCEL_REQUESTED"
}
```

---

## 7. 恢复任务

```http
POST /api/v1/tasks/{task_id}/resume
Authorization: Bearer <jwt>
```

响应：

```json
{
  "task_id": "task_01JZK9X2R6Z8M9",
  "status": "QUEUED",
  "resume_from_checkpoint_id": "ckpt_01JZKB"
}
```

---

# 八、Go 目录结构设计

生产级推荐使用清晰分层，不要把所有代码放在 `handler/service/model` 三层里。

```text
agent-platform/
├── cmd/
│   ├── api-service/
│   │   └── main.go
│   ├── runtime-worker/
│   │   └── main.go
│   ├── tool-gateway/
│   │   └── main.go
│   └── llm-gateway/
│       └── main.go
│
├── internal/
│   ├── app/
│   │   ├── api/
│   │   ├── worker/
│   │   ├── toolgateway/
│   │   └── llmgateway/
│   │
│   ├── domain/
│   │   ├── task/
│   │   ├── event/
│   │   ├── state/
│   │   ├── toolcall/
│   │   ├── checkpoint/
│   │   └── approval/
│   │
│   ├── usecase/
│   │   ├── createtask/
│   │   ├── runtask/
│   │   ├── resumetask/
│   │   ├── canceltask/
│   │   └── approvetool/
│   │
│   ├── infra/
│   │   ├── postgres/
│   │   ├── redis/
│   │   ├── temporal/
│   │   ├── nats/
│   │   ├── objectstore/
│   │   ├── llmclient/
│   │   └── toolclient/
│   │
│   ├── transport/
│   │   ├── http/
│   │   ├── grpc/
│   │   └── sse/
│   │
│   ├── observability/
│   │   ├── logging/
│   │   ├── metrics/
│   │   └── tracing/
│   │
│   ├── security/
│   │   ├── authn/
│   │   ├── authz/
│   │   └── tenant/
│   │
│   └── config/
│
├── pkg/
│   ├── errors/
│   ├── ids/
│   ├── clock/
│   └── jsonx/
│
├── db/
│   ├── migrations/
│   └── queries/
│
├── api/
│   ├── openapi.yaml
│   └── proto/
│
├── deploy/
│   ├── docker-compose.yml
│   ├── helm/
│   └── k8s/
│
├── scripts/
├── go.mod
└── README.md
```

---

# 九、核心 Go 代码设计

## 1. Task 状态枚举

```go
type TaskStatus string

const (
    TaskQueued          TaskStatus = "QUEUED"
    TaskRunning         TaskStatus = "RUNNING"
    TaskWaitingApproval TaskStatus = "WAITING_APPROVAL"
    TaskPaused          TaskStatus = "PAUSED"
    TaskFailed          TaskStatus = "FAILED"
    TaskRetryableFailed TaskStatus = "RETRYABLE_FAILED"
    TaskCancelRequested TaskStatus = "CANCEL_REQUESTED"
    TaskCancelled       TaskStatus = "CANCELLED"
    TaskCompleted       TaskStatus = "COMPLETED"
    TaskStoppedByLimit  TaskStatus = "STOPPED_BY_LIMIT"
)
```

---

## 2. Runtime 主接口

```go
type Runtime interface {
    RunTask(ctx context.Context, taskID string) error
    ResumeTask(ctx context.Context, taskID string, checkpointID string) error
    CancelTask(ctx context.Context, taskID string) error
}
```

---

## 3. Checkpoint 接口

```go
type CheckpointStore interface {
    Create(ctx context.Context, cp Checkpoint) error
    GetLatest(ctx context.Context, taskID string) (*Checkpoint, error)
    GetByID(ctx context.Context, checkpointID string) (*Checkpoint, error)
}
```

---

## 4. Tool Gateway 接口

```go
type ToolGateway interface {
    Call(ctx context.Context, req ToolCallRequest) (*ToolCallResult, error)
}
```

---

## 5. Budget 检测

```go
type BudgetChecker interface {
    CheckBeforeStep(ctx context.Context, taskID string) error
    RecordLLMUsage(ctx context.Context, taskID string, usage TokenUsage) error
    RecordToolUsage(ctx context.Context, taskID string, toolName string) error
}
```

---

## 6. Loop Detector

```go
type LoopDetector interface {
    BuildFingerprint(step AgentStep) string
    Detect(ctx context.Context, taskID string, fingerprint string) (*LoopDetectionResult, error)
}
```

---

# 十、生产级关键机制

## 1. 幂等机制

必须幂等的操作：

| 操作          | 幂等方式                           |
| ----------- | ------------------------------ |
| 创建任务        | client_request_id              |
| 启动 workflow | workflow_id = task_id          |
| 工具调用        | tenant_id + idempotency_key 唯一 |
| 审批          | call_id 唯一审批                   |
| cancel      | 多次调用返回当前状态                     |
| resume      | checkpoint_id + task_id 幂等     |
| event 推送    | event_id 单调递增                  |

工具调用幂等键建议：

```text
hash(task_id + step_id + tool_name + normalized_arguments)
```

对于发送邮件、删除文件、修改数据库等有副作用工具，必须做到：

```text
先写 tool_call PENDING
再执行外部工具
再写 result
恢复时先查 tool_call，不直接重试外部工具
```

---

## 2. 事务边界

生产级必须避免这个错误：

```text
DB transaction 里调用 LLM 或外部工具
```

正确方式：

```text
事务 1：写 checkpoint BEFORE_TOOL
外部调用：执行工具
事务 2：写 tool result + checkpoint AFTER_TOOL + event
```

原因：

* 外部调用耗时不可控；
* 长事务会锁表；
* 网络失败会导致事务悬挂；
* 不利于恢复。

---

## 3. Outbox 模式

创建任务时：

```text
DB transaction:
  insert agent_task
  insert agent_state
  insert agent_event
  insert task_outbox event=START_WORKFLOW
```

后台 outbox worker：

```text
扫描 PENDING outbox
  -> 启动 Temporal Workflow / 发布 NATS 消息
  -> 成功后标记 SENT
  -> 失败后 retry_count + 1
```

这样可以避免：

```text
任务写入成功，但队列消息丢失
```

---

## 4. 多租户隔离

生产级要求：

* 所有核心表带 `tenant_id`；
* 所有查询必须加 `tenant_id`；
* 中间件从 JWT 中解析 tenant；
* 不信任前端传来的 tenant_id；
* Worker 执行前再次校验 task tenant；
* 工具权限按 tenant 配置；
* 高风险审批按 tenant 策略执行；
* trace、日志中记录 tenant_id，但避免记录敏感参数。

---

## 5. 可观测 Trace

每个 task 必须有：

```text
trace_id
task_id
tenant_id
user_id
workflow_id
```

每个核心步骤创建 span：

```text
task.create
workflow.start
agent.step
llm.call
tool.call
checkpoint.create
approval.wait
approval.decide
budget.check
loop.detect
artifact.save
```

日志字段统一：

```json
{
  "level": "info",
  "service": "runtime-worker",
  "task_id": "task_01JZK9",
  "tenant_id": "tenant_001",
  "event_type": "STEP_STARTED",
  "trace_id": "trace_abc",
  "span_id": "span_001",
  "message": "agent step started"
}
```

---

# 十一、生产部署设计

## 1. Kubernetes 部署单元

```text
api-service Deployment
runtime-worker Deployment
tool-gateway Deployment
llm-gateway Deployment
event-service Deployment
outbox-worker Deployment
approval-worker Deployment

PostgreSQL StatefulSet / 云数据库
Redis StatefulSet / 云 Redis
NATS JetStream / Kafka
Temporal Server
OpenTelemetry Collector
Prometheus
Grafana
Tempo / Jaeger
MinIO / S3
```

---

## 2. Worker 扩缩容

Runtime Worker 支持横向扩展：

```text
runtime-worker replicas = N
每个 worker 限制 MaxConcurrentTasks
Temporal 负责分发 workflow task / activity task
```

扩容指标：

* pending task 数；
* activity queue backlog；
* CPU；
* LLM call latency；
* tool call latency；
* task p95 duration。

---

## 3. 配置示例

```yaml
server:
  port: 8080
  read_timeout: 5s
  write_timeout: 30s

postgres:
  dsn: ${POSTGRES_DSN}
  max_open_conns: 50
  max_idle_conns: 20
  conn_max_lifetime: 30m

redis:
  addr: ${REDIS_ADDR}

temporal:
  host_port: ${TEMPORAL_HOST_PORT}
  namespace: agent-prod
  task_queue: stableagent

runtime:
  max_steps: 50
  max_tokens: 200000
  max_tool_calls: 40
  step_timeout: 2m
  llm_timeout: 60s
  tool_timeout: 60s

observability:
  otel_endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT}
```

---

# 十二、技术难点与项目亮点

## 难点一：长任务可恢复执行

### 业务背景

Agent 任务不是一次 LLM 请求，而是多 step、多工具、多 checkpoint 的长流程。任何一步失败都不能让任务从头开始。

### 为什么困难

* LLM 输出不可完全复现；
* 工具调用可能有副作用；
* Worker 可能中途重启；
* 用户可能 cancel；
* 高风险工具可能等待审批很久；
* state、event、checkpoint 必须一致。

### 可选方案

| 方案                | 优点       | 缺点           |
| ----------------- | -------- | ------------ |
| 自研队列 + checkpoint | 可控，适合学习  | 复杂恢复逻辑需要自己实现 |
| Temporal Workflow | 长任务恢复能力强 | 引入额外组件       |
| Kafka 状态机         | 吞吐高      | 开发复杂度高       |
| 全量重跑              | 简单       | 不能处理副作用工具    |

### 最终方案

生产级推荐：

```text
Temporal Workflow + PostgreSQL Checkpoint + ToolCall 幂等记录
```

### 选择理由

Temporal 负责长任务调度、Activity 重试、超时和 workflow 持久化；PostgreSQL 保存业务可审计数据；ToolCall 幂等表解决副作用工具重复执行问题。

### 实现步骤

1. `task_id` 作为 `workflow_id`；
2. 每个 step 是一个 workflow 阶段；
3. LLM call 是 Activity；
4. tool call 是 Activity；
5. 高风险审批使用 Workflow Signal 等待；
6. 每次 LLM/tool call 前后写 checkpoint；
7. Worker 重启后 workflow 自动恢复；
8. resume 时加载最近 checkpoint；
9. 工具调用前检查 idempotency_key；
10. UI 通过 event 表展示 timeline。

### 测试方法

| 测试               | 预期                            |
| ---------------- | ----------------------------- |
| Worker 在 LLM 前崩溃 | 从 BEFORE_LLM checkpoint 恢复    |
| Worker 在 LLM 后崩溃 | 复用已保存 LLM 输出                  |
| Worker 在工具调用后崩溃  | 不重复执行副作用工具                    |
| 审批等待期间重启         | workflow 继续等待 signal          |
| 用户 cancel        | workflow 停止，task 进入 CANCELLED |

### 优化指标

| 指标      |   优化前 |               优化后 |
| ------- | ----: | ----------------: |
| 失败后恢复方式 |  从头重跑 | 从最近 checkpoint 恢复 |
| 重复工具调用  |   高风险 |              幂等控制 |
| 故障定位    |   查日志 |  timeline + trace |
| 长任务可靠性  | 中断即失败 |               可恢复 |

### 面试讲法

> 这个项目的核心是把 Agent 执行变成一个可恢复的 Workflow。Temporal 负责长任务生命周期，PostgreSQL 负责业务审计和 checkpoint，ToolCall 表通过 idempotency_key 防止重复执行有副作用工具。这样 Worker 崩溃、网络失败、审批等待都不会导致任务丢失。

---

## 难点二：工具调用网关和高风险审批

### 业务背景

Agent 会调用外部工具，但工具有风险。例如发送邮件、删除数据、修改配置都不能让模型直接执行。

### 为什么困难

* LLM 输出不能完全信任；
* 工具参数可能危险；
* 不同租户审批策略不同；
* 工具有副作用；
* 审批后还要恢复原任务；
* 所有调用必须审计。

### 可选方案

| 方案              | 优点     | 缺点    |
| --------------- | ------ | ----- |
| Agent 直接调用工具    | 简单     | 不安全   |
| 每个工具自己做审批       | 灵活     | 逻辑分散  |
| 统一 Tool Gateway | 可控、可审计 | 实现复杂  |
| 所有工具都审批         | 安全     | 用户体验差 |

### 最终方案

```text
所有工具调用必须经过 Tool Gateway
Tool Gateway = schema 校验 + RBAC + risk engine + idempotency + approval + executor
```

### 实现步骤

1. Tool Registry 注册工具；
2. 每个工具定义参数 schema；
3. Agent 输出 tool call 后先交给 Gateway；
4. Gateway 规范化 arguments；
5. 生成 arguments_hash；
6. 生成 idempotency_key；
7. 检查用户和 tenant 权限；
8. 计算 risk_level；
9. LOW 自动执行；
10. HIGH 创建 approval 并暂停 task；
11. 用户 approve 后发送 workflow signal；
12. Tool Executor 执行真实工具；
13. 结果写入 ToolCall 和 AgentEvent。

### 测试方法

| 测试        | 预期                   |
| --------- | -------------------- |
| 调用只读工具    | 自动执行                 |
| 调用发送邮件工具  | 等待审批                 |
| 重复提交审批    | 只生效一次                |
| 无权限工具调用   | 返回 TOOL_NOT_ALLOWED  |
| 相同幂等键重复调用 | 不重复执行                |
| 审批拒绝      | Agent 收到 rejected 结果 |

### 优化指标

| 指标     |  优化前 |                优化后 |
| ------ | ---: | -----------------: |
| 高风险误操作 | 可能发生 |               必须审批 |
| 工具审计   |   分散 |      统一 ToolCall 表 |
| 接入新工具  | 重复开发 |        注册到 Gateway |
| 重复副作用  |  无保护 | idempotency_key 保护 |

### 面试讲法

> 我没有让 Agent 直接调用工具，而是设计了 Tool Gateway。所有工具调用都会经过参数校验、权限校验、风险识别和幂等处理。高风险工具会暂停 workflow，等待用户审批，通过后再用 signal 恢复执行。

---

## 难点三：可观测 timeline 与 trace 统一

### 业务背景

用户关心任务执行进度，开发者关心系统为什么失败。两者需要不同视角：

* 用户看 timeline；
* 开发者看 trace；
* 运维看 metrics；
* 审计看 event。

### 为什么困难

* Agent step 多；
* LLM 和工具调用链路长；
* 异步任务不在同一个 HTTP 请求内；
* Worker 可能重启；
* event、log、trace、metric 容易割裂。

### 最终方案

```text
task_id 作为业务主线
trace_id 作为技术链路主线
event_id 作为 timeline 主线
```

每个 event 都保存：

```text
task_id
tenant_id
event_type
trace_id
span_id
metadata
created_at
```

### 实现步骤

1. 创建任务时生成 trace_id；
2. task_id、trace_id 写入 context；
3. HTTP、Worker、LLM、Tool Gateway 全链路传递；
4. 每个关键步骤写 AgentEvent；
5. 每个关键步骤创建 OpenTelemetry span；
6. UI 查询 event 表展示 timeline；
7. 技术排查跳转 trace 后端查看耗时。

### 优化指标

| 指标       |  优化前 |                    优化后 |
| -------- | ---: | ---------------------: |
| 用户是否知道进度 |  不知道 |            timeline 可见 |
| 失败定位时间   |    高 | 可按 task_id/trace_id 定位 |
| 工具耗时分析   |   困难 |                span 可见 |
| 审计完整性    | 依赖日志 |             event 表持久化 |

### 面试讲法

> 我把 event 和 trace 分开设计。event 面向业务审计和 UI timeline，trace 面向技术排查。两者通过 task_id、trace_id、span_id 关联。用户能看到任务执行过程，开发者也能追踪 LLM 和工具调用耗时。

---

# 十三、必须项与可选项总结

## 生产级必须项

```text
Go
PostgreSQL
Redis
Temporal 或等价长任务工作流机制
Tool Gateway
Checkpoint
Idempotency Key
AgentEvent
Cancel / Resume
Approval
OpenTelemetry
结构化日志
Prometheus metrics
Docker
Kubernetes
JWT / OIDC
多租户隔离
Outbox
对象存储
```

## 可选项

```text
Kafka，可用 NATS JetStream 替代
gRPC，MVP 可先用 HTTP
Temporal，严格生产推荐；学习型项目可自研状态机
Kong / Envoy，MVP 可用 Nginx
Vault，MVP 可用云厂商 Secret Manager
Jaeger，可用 Tempo 替代
MinIO，本地开发可用，线上可用 S3
```

## 不建议生产使用的做法

```text
只用 goroutine 跑长任务，不持久化状态
工具调用直接由 Agent 发起
没有 idempotency_key
没有 checkpoint
没有 outbox
DB transaction 中调用 LLM 或外部工具
只靠日志排查，不写 AgentEvent
没有租户隔离
没有 token/step/tool budget
没有人工审批
```

---

# 十四、项目最终亮点

这个项目用 Go 做生产级 StableAgent，亮点不是简单调用大模型，而是围绕生产环境的稳定性、安全性和可观测性设计：

```text
1. Temporal + Checkpoint 实现长任务可恢复
2. Tool Gateway 实现工具调用安全边界
3. idempotency_key 防止副作用工具重复执行
4. AgentEvent + SSE 实现用户可见 timeline
5. OpenTelemetry 实现完整 trace
6. Budget + LoopDetector 防止 Agent 失控
7. Outbox 保证 DB 与异步消息一致
8. 多租户 + RBAC 支持企业级使用
9. Go Worker 支持高并发、可水平扩展
10. PostgreSQL JSONB 保存半结构化 Agent 状态和事件
```
